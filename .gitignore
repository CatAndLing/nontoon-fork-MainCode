# 只跟踪两个 VPM 包 + 生成器；工作区里的其它东西一律不进仓库。
_notes/
_verify-proj/
_e2e-proj/
_live/
_audit/
_refs/

# Unity / .NET 噪声
Library/
Temp/
obj/
Obj/
Logs/
UserSettings/
Build/
Builds/
.vs/
*.csproj
*.sln
*.user
*.zip

Thumbs.db
.DS_Store

# ── ⛔ 付费 / 第三方参考资产：**绝不允许进仓库，这是公开仓库！** ─────────────────
# 项目硬规则：「参考付费包只学机制，**资产/源码一行都不进我们的包**」。
# 实测踩到：`リアル影システム (for Avatar) PCSS For VRC/` 曾经就躺在工作区根下
# 且**没有被任何规则覆盖** ⇒ 一次 `git add -A` 就会把付费资产推上公开仓库。
# 前缀 `/` 表示锚定仓库根；下面两条一正一反互为兜底。
/リアル影システム*/
/*リアル影システム*/
*PCSS For VRC*/

# `_notes/` 里已经用目录规则排除了，但门禁脚本会把日志写在**仓库根**，单独收掉。
# （否则 `git status` 会被几十个 _verify-*.log 淹没，真正要提交的东西反而看不见。
#   **`git status` 不可信本身就是一类假绿** —— 这才是要收它的真正理由，不是洁癖。）
_verify-*.log
*.log

# `_VPM-nontoon-fork/` 是**另一个** git 仓库（只装 VPM 列表与 zip，不含源码），
# 不是本仓库的内容；留着它会让 `git status` 一直挂一个 `??` 目录。
_VPM-nontoon-fork/

# 测试床工程（一次性搭起来跑门禁用的，含 Library/ 缓存，不该进版本控制）：
#   _clean-proj/          —— 2026-09-17 那次**无效测量**用的工程（包目录在编译中被移走）
#   _clean-proj-outside/  —— 上面那次被移出去的包
#   _gate-proj/           —— 干净安装门禁（装置 I）的测试床，由 vrc-get + 标准模板搭起
# ⚠️ 门禁的**脚本与探针本体**要进版本控制（见 _notes/probes/NTCleanInstallProbe.cs 与
#    _notes/verify-clean-install.sh）；只有这些**可再生的测试床目录**排除。
_clean-proj/
_clean-proj-outside/
_gate-proj/
