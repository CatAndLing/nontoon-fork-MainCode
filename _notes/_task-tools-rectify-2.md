# 任务（第二批 · 根因定位）：三个症状 **只定位，不改实现**

> **为什么另开一份**：第一批 `_notes/_task-tools-rectify.md` 跑到 **39.9 万 token / 1.93 MB 日志**时
> 撞上网关 `429 Too Many Requests` 被打断（不是能力问题）。所以本批**故意只做一件事**。
> **省流要求**：按需读文件、控制单次工具输出量，**不要**把整仓库塞进上下文。

## 0. 第一批已经做到哪（**不要重做**）

| 项 | 状态 |
|---|---|
| 2.1 去死代码（工具包自渲线性深度图 C# 半个） | **基本完成**：`Runtime/NTSelfRealtimeShadow.cs` 591→110 行；`Editor/NTSelfRealtimeSetup.cs`、`NTSelfRealtimeShadowEditor.cs` 连带清理 |
| 2.1 的判据 | ✅ 已由用户侧复跑 `verify-all.sh B E H` → **三台全 exit 0** |
| 2.2 修假描述 | **改了一半**（`package.json` description + 一处检视面板文案） |
| 2.4 全盘 `verify-all.sh` | **由用户侧负责，不归你** |

⛔ **工作区里这 4 个文件是未提交改动，不要回退、不要重做**：
`nontoon-converter/Runtime/NTSelfRealtimeShadow.cs`、`Editor/NTSelfRealtimeSetup.cs`、
`Editor/NTSelfRealtimeShadowEditor.cs`、`nontoon-converter/package.json`。

## 1. 本批唯一目标：把**三个症状**的根因**定位到可复现证据**

⛔ **只定位，不改实现。** 允许你新增**探针**（`_notes/probes/` 下，具名、判据可失败），
允许跑现有的装置；**不允许**改 `NonToon/` 或 `nontoon-converter/` 的实现代码。

### (a) 夹克：噪点 + 针织质感异常
### (b) 头发：灰紫
> 已知收窄（**引用即可，不必重查**）：`_notes/对照与转换器状态-20260918.md` §2.2 末行写着
> —— **不是贴图烘焙**问题，已收窄到**着色 / 法线路径**。

### (c) 【新增·用户实报】透明容器内含物体
场景：一个**透明的袋子（或容器）里面有物体**，NonToon 下渲染出问题。
> 已知线索（**引用即可**）：
> - fork 有**自研两趟透明变体** `NonToonTwoPass.scshader`，由 `tools/make-twopass.mjs` 生成；
>   它插入的 4 个 `_Pre*` 属性**与 lilToon 的 `_Pre*` 一一对应**，目的就是让转换器 1:1 拷贝。
> - ⚠️ **转换器尚未接线**（仍只产单趟）：`_notes/对照与转换器状态-20260918.md:133`。
> - ⚠️ 该变体**只在一个素材上做过数值验证**，**没有跨材质矩阵验证**：`NonToon/SCOPE.md:45`。
> - ⚠️ **必须区分的两件事**：两趟变体解决的是「**同一个 mesh** 的正/背面」；
>   而"袋内有物"是「**两个 mesh** 的关系」。后者还需要**队列/排序**正确
>   （`[NT-FIX 32]` 已做"照搬源材质生效队列"）。**两者叠加是否足够，我没有证据 —— 必须实测，不许推断。**

## 2. 交付格式（照这个写，别写散文）

每个症状给出**二选一**：

**① 定位到了** —— 可复现证据链：
- 哪个模块 / 哪个属性 / `文件:行号` / 哪一路输入算错 / 探针报告路径
- 一条**能独立失败**的最小复现（命令 + 退出码 + 关键输出）

**② 定位不到** —— 明确写：
- **已排除**哪些假设（每条都要有"怎么排除的"）
- **下一个该做的实验**是什么（具体到跑什么、看什么）

并逐条标注：**已验证 / 推断 / 未验证**。

## 3. 硬约束（违反即返工）

1. ⛔ **不改断言 / 判据让门禁变绿**（本仓库已因此吃过一次亏：装置 C 被改断言而不是退役）。
2. 判定**必须靠测量**（退出码 / 逐像素 / 序列化字段直读），不接受"我看了一下"。
3. ⛔ 本批**不要**接线两趟透明、**不要**改实现 —— 那是下一批。
4. ⛔ **不碰** `_VPM-nontoon-fork/docs/`（已发布 listing 与 zip，一个字节都别动）。
5. ⛔ 下列文件**由用户侧统一口径，你不要动**：
   `nontoon-converter/SelfLight.md`、`NonToon/SCOPE.md`、`README.md`、`_VPM-nontoon-fork/README.md`。
6. 跑 Unity 前确认没有别的 `verify-all` 在跑（`verify-all.sh` 自带锁文件）。
   用户的编辑器开在 `C:\VRC-File\unitypkg`（**另一个工程**），并行**安全**；⛔ 不要要求用户关掉它。
7. `_notes/` 被 `.gitignore` 整目录排除，但一部分文件**已在版本控制里** ⇒ 相关新增/改动要 `git add -f`。
8. 卡住时**如实说明卡在哪**（命令 + 退出码 + 输出），然后停。⛔ **不许用降级方案冒充原任务**。
9. **不要**替用户提交（`git commit`）；结束时给 `git status --short` 的真实输出。
